# Placeholder images

Every `.png` in this folder is a 1x1 transparent placeholder, committed only
so the main README's `<img>` tags resolve to a valid image instead of a
broken link. None of them are fake or mocked-up screenshots.

Replace each file with a real capture of the running application by
following the instructions in [`scripts/screenshots.md`](../../scripts/screenshots.md).
Keep the filenames identical so the README doesn't need any further edits:

- `banner.png`
- `dashboard-idle.png`
- `scan-running.png`
- `scan-complete.png`
- `findings-table.png`
- `html-report.png`
- `cli-scan.png`
